# 个人博客微信小程序系统 — 技术栈与部署分析

> 分析日期:2026-08-05,基于当前仓库代码(Spring Boot 2.7.18 / 微信小程序原生开发)。

## 一、技术栈

| 层 | 技术 |
| --- | --- |
| 后端 | Java 8 + Spring Boot 2.7.18 + Spring Data JPA + Maven |
| 数据库 | H2(默认内存模式,零安装,启动自动建表 + 初始化数据) |
| 认证 | 自研轻量 Token(内存实现,管理员 / 小程序用户双角色)+ 拦截器 |
| 管理后台 | 纯原生 HTML/CSS/JS(Spring Boot 静态资源托管,无前端框架、无外部依赖) |
| 小程序端 | 微信小程序原生开发(WXML/WXSS/JS),`rich-text` 富文本 + 内置 Markdown 转换器 |
| 部署形态 | 单 jar 可运行,一个包就是整个后端 |

特点:没有独立数据库、没有 Redis、没有前端工程化,属于"极简单体"架构。

## 二、本地运行

1. 安装 JDK 8+ 与 Maven 3.6+。
2. 构建并启动:

```bash
cd backend
mvn -s ../maven-settings.xml clean package -DskipTests
java -jar target/blog-miniapp.jar
```

3. 管理后台:http://localhost:8080/admin ,默认账号 `admin / admin123`。
4. 小程序端:微信开发者工具导入 `share-wxmal` 目录,把 `config.js` 的 `baseUrl` 指向后端地址;本地联调勾选"不校验合法域名"。

## 三、服务器部署

1. 准备 Linux/Windows 服务器,安装 JDK 8+。
2. 获取代码并构建 jar(注意 `target/` 已被 .gitignore 排除,jar 不会进仓库,需在服务器构建或本机构建后上传)。
3. 设置微信环境变量:

```bash
export BLOG_WECHAT_APPID="你的AppID"
export BLOG_WECHAT_SECRET="你的AppSecret"
```

4. 启动后端:

```bash
java -jar blog-miniapp.jar
```

5. 用 Nginx 反向代理 8080 端口并配置 HTTPS。
6. 持久化建议:将 `application.yml` 中 H2 改为文件模式(`jdbc:h2:file:./data/blog;MODE=MySQL;DB_CLOSE_DELAY=-1`),否则重启数据清空;并备份 `./uploads` 上传目录。
7. 小程序后台(mp.weixin.qq.com)配置 request / uploadFile / downloadFile 合法域名为你的 HTTPS 域名。
8. 提审发布前:修改默认密码、关闭 H2 控制台。

## 四、注意事项

- H2 内存模式数据在服务重启后会重置,正式使用请切换文件模式。
- 微信 AppID / AppSecret 通过环境变量注入,不要把真实值写死在配置文件里。
- 默认管理员密码 `admin123` 上线前必须修改。
- 图片上传目录为 `./uploads`,注意持久化与备份。
